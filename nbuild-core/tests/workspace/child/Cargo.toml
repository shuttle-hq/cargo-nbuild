[package]
name = "child"
version = "0.1.0"
edition = "2021"

[features]
default = ["one", "two"]
one = []
two = []

[dependencies]
libc = "0.2"
itoa = "1.0"
fnv = "1.0" # This dependency uses a custom lib path
